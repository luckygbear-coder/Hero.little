import { defineConfig } from 'vite'

export default defineConfig({
  // 這裡要放「GitHub 專案名稱」
  // 你的網址是 https://luckygbear-coder.github.io/Hero.little/
  // 所以 base 就是 "/Hero.little/"
  base: '/Hero.little/'
})
